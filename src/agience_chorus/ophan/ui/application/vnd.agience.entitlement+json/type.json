{
  "content_type": "application/vnd.agience.entitlement+json",
  "version": 1,
  "inherits": [
    "application/json"
  ],
  "description": "A commercial entitlement record describing what an operator is allowed to run and govern.",
  "context_schema": {
    "entitlement_id": "string — entitlement identifier",
    "account_id": "string — operator or billing account",
    "policy_id": "string — commercial policy identifier",
    "profile": "string — target packaging profile",
    "runtime_roles": "string[] — runtime roles allowed under this entitlement",
    "required_entitlements": "string[] — entitlements required for this profile",
    "operator": "object — verified operator identity and derived entitlements used to authorize the operation",
    "branding_scope": "string[] — branding or white-label surfaces in scope",
    "downstream_customer": "string? — downstream customer name or reference",
    "state": "string — active | pending | suspended | revoked",
    "issued_at": "string — ISO 8601 timestamp",
    "expires_at": "string? — ISO 8601 timestamp"
  },
  "ui": {
    "label": "Entitlement",
    "icon": "key-round",
    "color": "#2563eb",
    "badge_class": "bg-blue-100 text-blue-800",
    "tile_class": "ring-1 ring-blue-200/70",
    "modes": [
      "floating",
      "preview"
    ],
    "states": [
      "view"
    ],
    "default_mode": "floating",
    "default_state": "view",
    "is_container": false,
    "creatable": false,
    "description": "Commercial entitlement record owned by Ophan.",
    "resource_uri": "ui://ophan/vnd.agience.entitlement.html",
    "resource_server": "ophan"
  }
}
